;; Your own custom settings can go here
;; A comment begins with a semi-colon (;)
;;
;; A new line for settings can be used by uncommenting an existing
;; line below and customizing according to your preferences
;; please note that the system will attempt to find the best match
;; for your EQ install, but if you have it in a really weird location
;; you will want to set this up manually
;;
;; The typical format is: variable "value"
;;
;; *eq-path* - The location of your EQ install

(in-package :p99-patcher)

(setf
 *junk-sample-setting* "junk-sample-value"
 ;;*eq-path* "C:/Your/Custom/EQ/Path/Note/Forward/Slashes/"
 )
